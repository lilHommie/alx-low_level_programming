#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: is the integer in the question
 * Return: the abs value the integer
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);

	else
		return (-n);
}
